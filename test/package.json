{
    "name": "@partchat/test",
    "scripts": {
        "prettier": "prettier --check specs",
        "install-browsers": "playwright install --with-deps",
        "test": "playwright test"
    },
    "devDependencies": {
        "@playwright/test": "^1.36.1",
        "dotenv": "^17.0.0",
        "prettier": "^3.0.0"
    },
    "dependencies": {
        "slugify": "^1.6.6"
    }
}
