{
    "name": "@partchat/test",
    "scripts": {
        "install-browsers": "playwright install --with-deps",
        "test": "playwright test"
    },
    "devDependencies": {
        "@playwright/test": "^1.36.1",
        "dotenv": "^16.3.1"
    }
}
