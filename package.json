{
    "name": "partchat-end-2-end-tests",
    "scripts": {
        "install-browsers": "playwright install --with-deps",
        "test": "playwright test",
        "post-deploy": "(cd backend && npm ci && npm run dbpush && npm run build) && (cd frontend && npm ci && npm run build)"
    },
    "devDependencies": {
        "@playwright/test": "^1.35.1",
        "dotenv": "^16.3.1",
        "pm2": "^5.3.0"
    }
}
